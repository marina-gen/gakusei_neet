<!DOCTYPE html>
<html>
    <head>
        <meta charset="PixelMplus12-Regular">
    <title> クエスト追加</title>
    <%= stylesheet_link_tag "Quest_tuika", :media => "all" %>
    </head>

    <body>
        <face face="PixelMplus12-Regular">
        <header class="midasi">
          <!-- 日付 -->
        <span id="view_today"></span>

        <script type="text/javascript">
        document.getElementById("view_today").innerHTML = getToday();
        
        function getToday() {
            var now = new Date();
            var year = now.getFullYear();
            var mon = now.getMonth()+1; //１を足すこと
            var day = now.getDate();
            var you = now.getDay(); //曜日(0～6=日～土)
        
            //曜日の選択肢
            var youbi = new Array("日","月","火","水","木","金","土");
            //出力用
            var s = year + "年" + mon + "月" + day + "日 (" + youbi[you] + ")";
            return s;
        }
        </script>
        <!-- 日付ここまで -->
        </header>
        
        <main class="main">
            <%= form_with model: @schedules do |f| %>
           <form action="#" method="post">

                    <input type="kakomi-box1" name="name" placeholder="予定を追加" class="kakomi-box1">
                    <input type="kakomi-box1" name="startdate" placeholder="時間を追加" class="kakomi-box1">
                <textarea class="kakomi-box1"  type="search" name="memo" placeholder="メモは100字以内"></textarea>
            </form>
        </main>  
           
    </face>
    </body>
        <footer>
            <div class="footerInner">
                <a href="" class="btn btn-svg">
                    <svg>
                      <rect x="1.5" y="1.5" rx="0" fill="none" width="150" height="50"></rect>
                    </svg>
                    <span class="kinou">セーブ</span>
                    <% end %>
                </a>
                <a href="" class="btn btn-svg">
                    <svg>
                      <rect x="2" y="2" rx="0" fill="none" width="150" height="50"></rect>
                    </svg>
                    <span>戻る</span>
                  </a>
            </div>
        </footer>
